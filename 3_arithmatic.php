<?php 
    // arithmatic 
    // +, -, *, / 

    //Increment , dec
    // ++ , --

    // operator precedence
    // ()
    //**
    // * / %
    // + - 
    $x = 1;
    $y = 2;
    $z = null;
    $z = $x + $y;
    $z = $x - $y;
    $z = $x * $y;
    $z = $x / $y;
    $z = $x % $y;

    $counter = 0;
    $counter++;
    $counter--;
    $counter += 2;
    $counter -= 1;
    
?>